# Build artifacts
bin/
dist/
vendor/
coverage.out
coverage.html
/obs_exporter
/mockecs

# Logs
log/
*.log

# Docs build (mkdocs)
site/

# Local config & secrets — never commit
.env
*.local.yaml
.claude.local.md
.claude/settings.local.json

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# flux-capture's default output directory. Its files are raw Flux responses
# carrying real cluster hostnames, IPs and namespace names — never commit them.
/flux-capture/

# Local tooling state
.rtk/

# superpowers subagent-driven-development scratch workspace
.superpowers/

# Vendor reference documentation (Dell ObjectScale guides, REST API bundles).
# Kept locally for research; never committed — they are Dell's copyrighted
# material and this repository is public. Cite them by title and section
# instead, as docs/adr/ and docs/superpowers/specs/ do.
docs/references/
